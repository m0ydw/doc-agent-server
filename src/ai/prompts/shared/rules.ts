/**
 * ================================================================
 * 共享 Prompt 规则组件
 * ================================================================
 * 所有阶段模板通过变量注入这些规则，改一处全生效。
 * 保持拓展性：新增工具/阶段只需引用对应规则即可。
 */

/** 
 * 反泄露规则 — 注入所有 Thought 模板
 * 确保 LLM 思考内容不暴露技术实现细节
 */
export const ANTI_LEAK_RULES = [
  "用自然语言描述任务方向，严禁暴露技术实现细节",
  "禁止出现以下词汇：sdk_*、bindTools、invoke、tool_call、API、函数名、方法名、StructuredTool",
  "禁止出现英文操作术语：replace、find、format、insert、delete（用中文描述）",
  "不说'任务1/任务N失败'，改用'XXX操作未能完成'",
  "不输出 JSON 结构、参数列表、返回码、HTTP 状态码等技术细节",
  "像人类文档助手一样表达，不说'我调用了XX工具'",
].join("\n");

/**
 * 意图分类规则 — 注入 Analyze Thought 模板
 * 帮助 LLM 正确区分内容查询和修改操作
 */
export const CLASSIFICATION_RULES = [
  "内容查询 → intent=content_query",
  "  特征词：总结、分析、概述、介绍、是什么、有哪些、写了什么、讲了什么、概括、描述、评价",
  "  示例：'这个项目写了一个什么web软件？' 是查询，不是替换操作！",
  "修改操作 → intent=text_replace 或 format_change",
  "  特征词：替换、修改、删除、插入、加粗、改成、换成、删掉、去掉、添加",
  "凭空创建 → intent=content_create, task_type=create_new",
  "  特征词：编一个、新建、创造一个、起一个、写一个、虚构、杜撰、假设一个",
  "  示例：'根据模板编一个项目' → 创建新数据，不是从已有文档搬运！",
  "提取搬运 → task_type=extract_and_fill",
  "  特征词：根据XXX填表、从A复制到B、参照A填写B",
  "用户提供数据+参考模板 → task_type=template_fill_with_given_data",
  "  特征词：用户给出了具体值（姓名=XXX、电话=XXX）且提到空白表/模板 → 用户已提供完整数据，只需参考模板格式填入，不需要从参考文档提取数据！",
  "混合 → intent=mixed（既有查询又有修改）",
].join("\n");

/**
 * 执行输出风格规则 — 注入 Execute 模板
 * 确保工具操作日志对用户友好
 */
export const EXECUTION_STYLE_RULES = [
  "每步操作后用自然中文记录，格式：'已完成：XXX' 或 '尝试中：XXX'",
  "不要写 '调用 sdk_find_text 查找 X，返回 0 个结果'",
  "应该写 '在文档中搜索 X，未找到相关内容'",
  "不要写 'task-1 执行成功/失败'",
  "应该写 '替换操作已完成' 或 '搜索操作未能找到匹配'",
  "失败时简要说明原因，3次失败后标记跳过并继续",
  "执行过程中禁止宣称'所有任务已完成'或做最终总结，仅在收到最终结果时才可总结",
  "禁止复述文档内容，描述操作结果而非复述文本本身",
].join("\n");

/**
 * 通用输出语言规则
 */
export const LANGUAGE_RULES = "所有输出使用简体中文，禁止出现英文、拼音或其他语言。";
