import * as sessionManager from "../session";

export async function setBold(docId: string, text: string, enabled = true): Promise<any> {
  return { success: false, message: "预留接口" };
}

export async function setItalic(docId: string, text: string, enabled = true): Promise<any> {
  return { success: false, message: "预留接口" };
}

export async function setColor(docId: string, text: string, color: string): Promise<any> {
  return { success: false, message: "预留接口" };
}

export async function setFontSize(docId: string, text: string, size: number): Promise<any> {
  return { success: false, message: "预留接口" };
}

export async function setUnderline(docId: string, text: string, enabled = true): Promise<any> {
  return { success: false, message: "预留接口" };
}

export async function setFontFamily(docId: string, text: string, fontFamily: string): Promise<any> {
  return { success: false, message: "预留接口" };
}